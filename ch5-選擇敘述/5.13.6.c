/* selDebug6.c */

#include <stdio.h>
#include <stdlib.h>

int main () {
	int num = 40;
	if(num > 0) {
		printf("It is a positive integer.\n");
	}else if (num == 0) {
		printf("It is 0.\n");
	}else {
		printf("It is a negative intrger.\n");
	return 0;
	}
}
